<div class="depot-form">
<%  form_for(@user) do |f| %>
  <%= f.error_messages %>
      <fieldset>
  <legend>Editing user</legend>
  <div>
    <%= f.label :name %>:
    <%= f.text_field :name, :size => 40 %>
  </div>
  <div>
    <%= f.label :user_password, 'Password' %>:
    <%= f.password_field :password, :size => 40 %>
  </div>
  <div>
    <%= f.label :user_password_confirmation, 'Confirm' %>:
    <%= f.password_field :password_confirmation, :size => 40 %>
      </div>
      <div>
       <%= f.label :user_type %>
       <%= select('user', 'type', options_for_select(@categories, @user.type)) %>
      </div>
      <div>
        <%= f.submit "Edit User" , :class => "submit" %>
      </div>
    </fieldset>
<% end %>
</div>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
